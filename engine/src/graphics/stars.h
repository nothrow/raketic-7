#pragma once

// Procedural star background with parallax effect
// Stars are generated deterministically based on camera position

void stars_initialize(void);
void stars_draw(float camera_x, float camera_y);


